/.DS_Store

# Build artifacts
build/
*.xcuserstate
*.xcuserdatad

# Sparkle EdDSA keys (private key should never be committed!)
keys/eddsa_private_key.pem

# Releases (ZIPs e archives não devem ser commitados)
releases/
releases/*.zip
releases/*.xcarchive
releases/DerivedData
releases/export/
releases/.sha256
release/
